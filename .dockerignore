node_modules
.git
.gstack
*.md
!README.md
.env

# src/ and tsconfig.json are deliberately NOT ignored: Dockerfile.mcp compiles
# from source because dist/ is a build artifact and is not committed. The root
# Dockerfile copies only dist/, so keeping them in the context costs nothing.
